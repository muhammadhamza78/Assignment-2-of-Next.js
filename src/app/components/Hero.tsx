import styles from "./Hero.module.css";

const HeroSection = () => {
    return (
        <div>
            <h1 className={styles.heading}>My first website</h1>
        </div>
    );
};

export default HeroSection;
